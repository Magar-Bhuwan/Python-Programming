# lists are mutable and it stores the different data as an array

lists = ["Apple", "Elephant", "Cat", "Human", 45, 45.78, "Ram", "Aeroplane"]
print(lists[2])

lists[0] = "Gopal"      #Unlike Strings lists are mutable(changeable)
print(lists)